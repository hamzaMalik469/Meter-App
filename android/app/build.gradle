plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
    id("dev.flutter.flutter-gradle-plugin")
    id("com.google.gms.google-services") // ✅ Firebase plugin
}

android {
    namespace = "com.example.meter_app"
    compileSdk = 35 // or flutter.compileSdkVersion

    ndkVersion = "27.0.12077973" // or use flutter.ndkVersion if declared

    defaultConfig {
        applicationId = "com.example.meter_app"
        minSdk = 23 // or flutter.minSdkVersion
        targetSdk = 35 // or flutter.targetSdkVersion
        versionCode = 1 // or flutter.versionCode
        versionName = "1.0.0" // or flutter.versionName
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = "1.8"
    }

    buildTypes {
    getByName("release") {
        minifyEnabled = false // ✅ Correct Kotlin DSL
        shrinkResources = false
        signingConfig = signingConfigs.getByName("debug")
    }
}

}

flutter {
    source = "../.."
}

dependencies {
    implementation(platform("com.google.firebase:firebase-bom:33.16.0"))
    // Add more Firebase dependencies as needed, e.g.:
    // implementation("com.google.firebase:firebase-auth")
    // implementation("com.google.firebase:firebase-firestore")
}
